# 카카오 로그인 이메일 설정 가이드

## 문제
카카오 로그인 시 이메일 정보가 DB에 저장되지 않는 문제

## 해결 방법

### 1. 카카오 개발자 콘솔에서 이메일 동의 항목 활성화

1. **카카오 개발자 콘솔 접속**
   - https://developers.kakao.com 접속
   - 내 애플리케이션 선택

2. **제품 설정 > 카카오 로그인**
   - 카카오 로그인 활성화 상태 확인

3. **동의항목 설정**
   - "동의항목" 탭 클릭
   - **필수 동의** 또는 **선택 동의**에서:
     - **이메일** 항목 찾기
     - **활성화** 상태로 변경
     - **필수 동의** 또는 **선택 동의** 선택
     - **저장** 버튼 클릭

4. **앱 키 확인**
   - **앱 키** 탭에서 확인:
     - REST API 키
     - Native App Key (현재 사용 중: `***REMOVED_KAKAO_KEY***`)

### 2. 앱에서 이메일 요청

현재 코드는 이미 이메일을 요청하도록 설정되어 있습니다:
- `getKakaoProfile()` 함수가 프로필 정보(이메일 포함)를 가져옵니다
- 이메일 정보가 없으면 로그에 경고가 표시됩니다

### 3. 서버에서 이메일 저장

서버에서 로그인 API를 구현할 때 이메일 정보를 함께 저장해야 합니다:

```java
// 스프링부트 예시
@PostMapping("/auth/login")
public ResponseEntity<?> login(@RequestBody LoginRequest request) {
    // request.email을 DB에 저장
    User user = userService.findOrCreate(
        request.provider,  // 'google' or 'kakao'
        request.email,
        request.nickname,
        request.token
    );
    
    return ResponseEntity.ok(user);
}
```

### 4. 확인 방법

1. **앱에서 카카오 로그인 시도**
2. **터미널 로그 확인**:
   ```
   🔍 [시간] [Kakao]
   {
     "event": "Profile fetched",
     "email": "user@example.com",  // 이메일이 있으면 성공
     "hasEmail": true
   }
   ```
3. **이메일이 없으면**:
   - 카카오 개발자 콘솔에서 이메일 동의 항목이 활성화되어 있는지 확인
   - 사용자가 이메일 동의를 거부했는지 확인

## 주의사항

- **이메일이 없는 경우**: 사용자가 이메일 제공을 거부했거나, 카카오 계정에 이메일이 등록되지 않은 경우
- **동의 항목 변경**: 동의 항목을 변경하면 기존 사용자에게는 재동의가 필요할 수 있습니다
- **테스트**: 카카오 개발자 콘솔에서 테스트 계정을 사용하여 이메일이 정상적으로 전달되는지 확인

## 현재 코드 상태

✅ 카카오 로그인 시 프로필 정보(이메일 포함) 가져오기
✅ 이메일 정보를 로그로 출력
✅ 서버에 이메일 정보 전송 (API 호출)
⚠️ 서버에서 이메일을 DB에 저장하는 로직 필요








